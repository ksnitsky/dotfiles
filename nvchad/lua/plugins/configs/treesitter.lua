local options = {
  ensure_installed = { "lua", "ruby", "ocaml" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },
  -- rainbow = {
  --   enable = false,
  --   extended_mode = true,
  --   max_file_lines = nil,
  -- },
  -- matchup = {
  --   enable = true,
  --   -- options
  -- },

  -- indent = {
  --   enable = true,
  --   disable = {},
  -- },
}

return options
