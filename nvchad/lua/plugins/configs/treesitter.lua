local options = {
  ensure_installed = { "lua", "ruby" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },
  -- rainbow = {
  --   enable = false,
  --   extended_mode = true,
  --   max_file_lines = nil,
  -- },
  matchup = {
    enable = true,
    -- enable_quotes = true,
  },
}

return options
